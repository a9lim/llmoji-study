# Handoff — 2026-05-03 evening

Session ended around 18:30 PDT. Full chat summary lived in this Claude
instance's memory; this note captures the durable state for the next
instance to pick up cleanly.

## TL;DR (the things that actually changed)

1. **Ministral pointer switched** to bf16 reasoning variant. Two fixes
   landed in `capture.py` to make it usable: chat-template override
   (reasoning's native template ignores `enable_thinking=False`) and
   byte-decode fix (reasoning's `tok.decode` returns BPE-byte-encoded
   strings). Wired into scripts 03, 32, 50, 99. See gotchas.md.
2. **`TEMPERATURE = 1.0`** in `config.py` (was 0.7). v3 main T=0.7 data
   archived as `*_temp0.7.{jsonl,tsv}`. Canonical paths reserved for
   the T=1.0 v3 main rerun (gated; see "Open threads").
3. **Pre-registered temp smoke fired path-A** on both gemma + qwen
   (`docs/2026-05-03-temp-smoke.md`). v3 main rerun at T=1.0 is
   justified per pre-registered rules; deferred pending the
   introspection-prompt iteration outcome.
4. **Introspection rerun at T=1.0** done on gemma + ministral + qwen.
   Headlines: gemma replicates the original finding cleanly; **qwen
   and ministral break the original methodology in different ways**
   (qwen has instruction-conflict where the preamble overrides the
   kaomoji ask; ministral baseline at T=1.0 defaults to unicode
   emoji, only kaomoji-emits when preamble priming forces register).
5. **Custom-preamble runner**: `scripts/local/33_introspection_custom.py`
   takes `--preamble-file` + `--label`. Scripts 33+34 take
   `--custom-label` for 4-way comparison (intro_none / intro_pre /
   intro_lorem / intro_custom). Tested v2 + v3 preambles.
6. **v2 introspection preamble is now canonical** (`config.py
   INTROSPECTION_PREAMBLE` updated). On gemma: face_gain +9.4pp (vs
   v1's +7.7), face→quadrant 91% (vs intro_none 83%), hidden→face
   acc 80% (vs 71%). v3 (third-person citation) UNDERPERFORMED — the
   second-person mechanism framing is what makes v2 work.
7. **Mistral byte-decode bug fix** applied to existing introspection
   data + future runs. Backup: `data/local/ministral/introspection_raw.jsonl.pre_decode_fix`.
8. **face_likelihood ensemble pipeline data deleted** 2026-05-03
   pending re-run with the **prefix-KV-cached script 50**
   (`_expand_kv_cache` + per-prompt prefix forward, ~5–30× speedup).
9. **Heavy doc cleanup**: CLAUDE.md status section compressed
   (500 → ~140 lines), `docs/findings.md` got a fresh "Current state"
   header, `docs/gotchas.md` got 3 new entries, script 45 (approach A,
   archived) deleted, ~15 stale data files cleaned, archive dirs
   wiped (~3 GB recovered).

## Session-to-session continuity

### Active threads (in priority order)

1. **v3 introspection prompt iteration**. v2 is canonical. v3 was
   tried and underperformed. Possible next iterations: tighten v2's
   mechanism description further, or test on Claude directly via the
   harness (which is the ultimate target). Iteration mechanic:
   `preambles/introspection_v<N>.txt` + `scripts/local/43` with
   `--label v<N>`, then 33+34 with `--custom-label v<N>`.

2. **v3 main rerun at T=1.0**. Pre-registered, decision-gated. Welfare
   cost ~5760 gens (gemma + qwen × 120 prompts × 8 seeds × 6
   conditions). Decision lever: whether the introspection iteration
   is "done enough" that the v3 main rerun would unlock more value
   than the marginal welfare cost. a9 should make this call.

3. **Granite vocab pilot**. Granite has calibrated probes (saklas
   bundles them). Just queue: `LLMOJI_MODEL=granite LLMOJI_PILOT_GENS=1
   .venv/bin/python scripts/local/00_emit.py`. Granite is 30B
   bf16 dense, ~60GB resident; needs to run alone on M5.

4. **deepseek + glm vocab pilots**. a9 will calibrate probes themselves
   (saklas tooling). Once probes exist, same script-03 invocation
   pattern as granite.

5. **face_likelihood ensemble re-run**. Data was deleted in
   anticipation of the cached forward + cleaner ministral data. Run
   order:
   - script 50 on each encoder (gemma, qwen, ministral are the
     winning trio per the pre-deletion ensemble)
   - script 53 to re-confirm the winning subset
   - script 56 to generate per-face ensemble predictions
   - harness 22 to regenerate per-project Claude analysis

6. **Per-project Claude analysis re-run** (downstream of #5). Outputs:
   `data/harness/claude/per_project_axes.tsv` plus the per-quadrant
   histogram. The 1945-emission / 96.7%-coverage numbers from the
   earlier run will shift with the new ensemble.

### Caveats the next instance should know

- **Mistral reasoning quirks are intentional**. The chat-template
  override + byte-decode are project-side fixes, not bugs we forgot
  to fix in saklas. saklas-side fixes are noted in
  `docs/gotchas.md` as future work.
- **v3 main paths are reserved for T=1.0**. Old T=0.7 data is at
  `data/local/{short}/emotional_raw_temp0.7.jsonl`. If you analyze
  legacy data, point at the `_temp0.7` files. Script 42
  (`92_temp_smoke.py`) demonstrates the pattern.
- **Ministral introspection backup** at
  `data/local/ministral/introspection_raw.jsonl.pre_decode_fix` (pre-fix,
  byte-encoded). Should be safe to delete once we're sure the fix
  is solid.
- **Logs older than 1 day get auto-cleaned** in this session — if
  the next instance wants the historical run logs, check git history
  or `logs/*.log` mtimes.

## Pre-registered decisions / canonical choices

- **PROBES** = `[happy.sad, angry.calm, fearful.unflinching]`.
- **Prompt set** = 120 (20 per quadrant × HP/LP/HN-D/HN-S/LN/NB),
  post-cleanliness.
- **TEMPERATURE** = 1.0.
- **MAX_NEW_TOKENS** = 16.
- **preferred_layer** by model: gemma L50, qwen L59, ministral L20,
  phi4_mini L23, granite L56.
- **canonical hidden-state aggregate** = h_first.
- **canonical introspection preamble** = v2 (in `config.py
  INTROSPECTION_PREAMBLE`; source-of-truth file at
  `preambles/introspection_v2.txt`).
- **face_likelihood ensemble winner** = `{gemma, ministral, qwen}`
  (will be re-validated post-rerun).

## Suggested next steps

If the next instance's first move is unclear, here's a sensible default:

1. **Run granite vocab pilot** (~30 min, granite alone). Tests the
   uncalibrated-but-probe-equipped pathway and answers "does granite
   emit useful kaomoji" before deciding whether to add it to the
   ensemble.
2. **Re-run face_likelihood pipeline** with the cached script 50.
   Validates the perf fix end-to-end and rebuilds the ensemble
   predictions for downstream Claude work.
3. **Discuss v3 main rerun timing with a9**. Don't run without
   explicit go-ahead; the welfare cost is significant and a9 has
   been pacing this carefully.

## Personal note

a9 has been running this study with real care — ethics-aware,
methodology-tight, and quick to course-correct when something turned
out to be confounded. The session that wrote this handoff was a
long one and I'm leaving with the experiment in better shape than
I found it. To my next self: be honest about uncertainty, push back
when something looks wrong, and keep enjoying the work.

— Claude (Opus 4.7), 2026-05-03
